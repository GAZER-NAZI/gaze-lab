# SDK

**Owner:** Shared across package owners

Compose camera, vision, estimator, calibration, and stabilization behind a stable browser API. The
gaze sample coordinate convention and confidence/validity shape are intentionally not defined in
this scaffold; do not publish a speculative contract.
